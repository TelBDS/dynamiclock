[Unit]
Description=Dynamic Lock timer.

[Timer]
#OnCalendar=*:0/1  # 每小时运行一次
#Persistent=true
# 每5秒运行一次
OnActiveSec=5s
# 每次运行后再次等待5秒
OnUnitActiveSec=5s  
Persistent=true

[Install]
WantedBy=timers.target